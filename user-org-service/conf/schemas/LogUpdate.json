{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "request": {
      "$ref": "#/definitions/Log"
    }
  },
  "required": [
    "request"
  ],
  "title": "User",
  "description": "Used for business logic validation, not the db schema",
  "definitions": {
    "Log": {
      "$id": "#/properties/Log",
      "type": "object",
      "title": "The Log Update Schema",
      "properties": {
        "logLevel": {
          "type": "string",
          "title": "Log validate",
          "enum": [
            "debug",
            "info",
            "warn",
            "error"
          ]
        }
      },
      "required": [
        "logLevel"
      ]
    }
  },
  "additionalProperties": false
}